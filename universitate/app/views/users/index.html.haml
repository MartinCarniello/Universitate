%h1

= render 'searchlight_form', search: @search, url: users_path

%h2 Results

.w3-container
  %ul.w3-ul.w3-card-4
    - @users.each do |user|
      %li.w3-padding-16
        = link_to users_path(user), class: 'view-button btn btn-info btn-lg pull-right' do
          = I18n.t("views.users.index.view_button")
        = image_tag user.avatar_url, class: 'w3-left w3-circle w3-margin-right', style: "width: 100px;"
        %br/
        %span.w3-xlarge
          = user.display_name
        %br/
        %h4.grey
          = user.subjects.map(&:name).join(", ")

= link_to "Sign out", destroy_user_session_path, method: :delete
