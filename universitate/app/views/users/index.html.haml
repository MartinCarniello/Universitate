%h1

= render 'searchlight_form', search: @search, url: users_path

%h2 Results

.w3-container
  %ul.w3-ul.w3-card-4
    - @users.each do |user|
      %li.w3-padding-16
        = link_to users_path(user), class: 'btn btn-info btn-lg pull-right' do
          View
        = image_tag 'http://bootdey.com/img/Content/user-453533-fdadfd.png', class: 'w3-left w3-circle w3-margin-right', style: "width: 100px;"
        %span.w3-xlarge
          = user.display_name
        %br/
        %span
          = user.try(:subjects)

= link_to "Sign out", destroy_user_session_path, method: :delete
